#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int test_cases;
    scanf("%d",&test_cases);
    while(test_cases >0)
    {
        int number;
        int array[32];
        scanf("%d",&number);
        int i =32;
        while(number>0)
        {
            if(number%2==0)

        }
        test_cases--;
        
    }
}
